---
name: inquiry
slash: true
description: "Используй для ЖИЗНИ вимарши и работы с ПОЛЕМ вопрошания — не постановка (это writing), а жизненный цикл: якорение, разрешение, закрытие, парковка, вытеснение, кристаллизация, привязка к превращению. Триггеры: «разбери вимарши», «поле вопрошания», «закрой/запаркуй вимаршу», «разреши вопрошание», «триаж вимарш», work the field of inquiry, resolve the question, много висящих вимарш в orient, закрытие сессии. Distinct from writing (постановка) и assembly (проход по всему графу): здесь вопросы доводятся до разрешения. Нужны тулы iskron_*."
---

# Вопрошание в Искроне

Вимарша — не стикер. Это записанное *натяжение с реальностью*, которое должно быть рано или поздно разряжено — разрешено, отпущено или кристаллизовано в форму. **writing** ставит вопрос; **этот** скилл ведёт его по жизни и работает с полем как целым.

Симптом, который скилл лечит: графы копят **висящие** вимарши — незаякоренные, неразрешённые, невидимые агентам, которые могли бы их разрядить. Поле свободных вопросов, не сходящееся к пониманию, — граф, который думает, но не *учится*.

## 1. Якорение обязательно

Незаякоренная вимарша — **невидимая** вимарша: она не всплывает ни в одном `lens="vimarshas"`, не распространяется ни на один узел. Каждой вимарше нужны две стрелки:

- **`vimarsha_of`** → узел(ы), *о которых* вопрос. Без него вопрос плавает.
- **`arose_from`** → наблюдение, *из которого* он увиден (узел, инцидент, прежняя вимарша). Без него у вопроса нет истока, к которому можно вернуться.

Вимарша, несущая **ожидание** (`posed_to` к роли, anga к превращению), нуждается в якоре вдвойне: агенты находят работу, ориентируясь на *контур*, а ни `anga`, ни `posed_to` не помещают вимаршу ни в чей контур — без `vimarsha_of` в территорию адресата она там невидима и не будет сделана. Минимум — контур, где живёт ожидаемая работа; лучше — точный феномен или крия внутри. (Детектор `unanchored` срабатывает пост-фактум; не полагайся на него — якори при постановке.) `posed_to` — живое **ребро-инбокс** к роли, чтобы адресат мог собирать «мои открытые вопросы»; запрещено к `pratibimba` (образ не ответит) и никогда не заменяет `vimarsha_of`.

Зеркальный провал так же реален: `vimarsha_of` **без** `posed_to` на вопросе, *ожидающем действий другого делателя*, — заякорено, видно в территории, ни в чьём инбоксе; делегирование, выродившееся в записку в пустоту. Ждёшь чьих-то действий — назови кого: 能, стюардящий контур работы (**writing**, Решение 2b — ищи по стрелке `steward` или `iskron_search(q="", node_type="karta")`, никогда по частичной выдаче корневых ролей в orient), и никогда `pratibimba`.

Нашёл висящую вимаршу (orient их флагует) — первый ход почти всегда заякорить, потом триаж.

## 2. Жизненный цикл — три исхода

Вимарша *жива*, пока держит открытое натяжение. Из живого поля она выходит ровно одной из трёх дверей:

| Исход | Что происходит | Как |
|---|---|---|
| **Разрешение** | на вопрос отвечено действующим/решающим узлом графа | стрелка `addressed_by` (вимарша → крия, роль, вимарша — или феномен по данности: различение, принцип, положение дел, вещь, метод отвечают, семя нет) + волевой `visarjana`. Если ответ — поведенческое утверждение («работает», «починено»), закрытию предшествует сверка реальностью с объявленной ступенью (дисциплина в vahta): прозой поведенческий провал не закрывается |
| **Смерть** | сознательно отпущена *без* ответа — была неверной, снялась, вытеснена | волевой `visarjana` с reasoning-строкой, *почему* умирает без ответа |
| **Кристаллизация** | прозрение становится стоячей формой | феномен с given_as, `arose_from` → вимарша и `addressed_by` от вимарши к нему (вырос из вопроса и отвечает на него — две стрелки), затем волевой `visarjana`, когда форма верно несёт содержание: жидкое (сомнение, наблюдение — подвижное) застывает в твёрдое (принцип, различение, метод) |

Кристаллизация — самый ценный исход: опыт не просто закрыт, а проткан в ткань — grundsatz/sinn применяется дальше через upadhi-рёбра к криям. Вимарша при этом закрывается (`visarjana`) и остаётся в графе как след вопроса, из которого вырос ответ. **«Вырос из» и «отвечает» — разные стрелки**: `arose_from` от формы к вимарше говорит, откуда форма взялась; `addressed_by` от вимарши к форме — что ею отвечено; кристаллизованная форма несёт обе, а на новый вопрос может отвечать узел, который стоит давно, — тогда это разрешение уже стоящим узлом, без новой формы.

Плюс два нетерминальных хода:

- **Вытеснение** — поздний вопрос поглощает этот: `supersedes` на замене, старая → `visarjana`.
- **Парковка** — нужна, но не сейчас: волевой `upeksha`, поставленный **сознательно**, у сомнения, контртезиса и подсказки одинаково. Парковка — *модус*, никогда не `attrs.parked=true` (подавление — антипаттерн); условие пробуждения лежит данным (`wake_condition`), а не состоянием. По канону запаркованное выходит из очереди адресата и из счёта открытого на поверхность «принято», где условие видно, — но оно **не отвечено**: превращению как разрешённая часть не засчитывается, и превращение, чьи живые части все запаркованы, стоит, а не завершено. Условие наступило — верни `chanda`. Парковка без условия пробуждения — тихое забвение под другим именем: нечего ждать — отпускай.

**Опровергнутый вопрос не закрыт.** Badhita на сомнении или риске значит «вопрос поставлен неверно» или «это невозможно» — повод переформулировать, вытеснить или отпустить; закрывает по-прежнему воля.

> Всё это — **модусы и стрелки**, никогда не attrs. `iskron_update(... volitive_mode=...)` двигает модус; `iskron_arrow(action="link", ...)` — для addressed_by / supersedes / arose_from. По канону матрицы стрелок `addressed_by` ведёт к крие, роли, вимарше **и к феномену по данности**: различение, принцип, положение дел (замер дал состояние — оно и есть ответ), вещь («где это, что это») и метод («как это делать») отвечают, семя не отвечает ничем — оно ещё не форма. Ответ-решение не дописывается абзацем в тело вопрошания, а ставится узлом со стрелкой ответа; **стрелка ничего не завершает** — она называет, чем отвечено, закрывает модус по носителю рода. Какие концы и с каким условием берёт поверхность, печатает `iskron_arrow(action="?")` — читай её, не память: её описание ответа-феномена может быть у́же канона (например, принцип, который применяет живая крия), и отказ приходит с причиной. Терминальная трипути ложится одним обновлением — без церемониальных промежуточных модусов. Каждая мутация update/delete/reconnect требует `basis_version` из свежего `iskron_look`; новая связь — нет.

## 3. Жанр задаёт характер разрешения — и ось закрытия

Жанр, под которым вопрос поставлен (writing), определяет, что *считается* его разрядкой, и какая ось несёт закрытие:

| Жанр | Разряжается чем | Ось закрытия |
|---|---|---|
| **samshaya** («верно ли?») | снятием неопределённости — решением, найденным узлом, проверкой (с названной ступенью: против графа или против мира) | воля: `visarjana` ∨ `virodha` (отказ); addressed_by называет, чем отвечено, и не закрывает; `upeksha` — парковка, не закрытие |
| **prati-paksha** («не согласен») | оценочным решением: предложение принято, преображено или отклонено | воля: `visarjana` ∨ `virodha` (отказ); `upeksha` — парковка |
| **hint** («будущий агент: прочти») | заведено, когда есть важное после сессии → правится in-place до «что стоит сейчас», своё отработавшее каждый вычищает → закрыто координатором превращения на приёмке. Одно семя на превращение, не журнал: ничего из того, что покажут orient, линзы и история | воля: `visarjana` ∨ `virodha`; `upeksha` — парковка |
| **risk** («что может пойти не так?») | митигирующей крией, grundsatz-инвариантом или *сознательным принятием*. Активный риск *стоит* в `virodha` — это его живой модус. Закрывают **только модусы**: `upeksha` (принят) ∨ `visarjana` (отпущен). **Принятие — акт владельца**: ты готовишь материал (что принимается, какой ценой, через что идёт) и адресуешь ему; сказанное им слово записываешь с провенансом. `addressed_by`/`realized_as` — информационны, не гейт: материализация риск НЕ закрывает (сбывшийся риск хуже открытого; `realized_as` → случившийся sachverhalt) | воля: `upeksha` ∨ `visarjana` |
| **vyabhichara** («случай мимо правила») | ремонтом, снимающим дефект, или уточнённым правилом | онтика: дефект есть, пока `vartamana`; закрытие — `atita` (устранён) ∨ `vikalpa` (дефекта не было — только через `badhita`) ∨ `nashta` (ушёл с носителем) ∨ `visarjana`; `virodha` конститутивен; `upeksha` при `vartamana` — принят как есть, акт владельца |
| **hetu-dosha** («рассуждение дефектно») | исправлением или отзывом аргумента | онтика: как у vyabhichara |
| **semantic-drift** («термин уплыл») | пере-фиксацией термина (正名) и сведением разъехавшихся употреблений — не согласием, что вопрос снят | онтика: как у vyabhichara |

Не можешь сказать, что разрядило бы вимаршу, — возможно, неверен жанр: перепроверь его (это решение **writing**).

**Принятое — не закрытое и не открытое.** Риск, дефект или дрейф в `upeksha` закрыты как работа и живы как запись: по канону детекторы открытого их не считают, они стоят на поверхности «принято» — кто принял, как быстро принятого прибывает, сколько деяний через него идёт, — и пробуждаются, когда на узле появляется новое вопрошание или новая стрелка через него. Это замысел канона; что из него поверхность уже несёт, читай из её отчёта, а не отсюда. «Сделал» — другое: ремонт увёл дефект в `atita` деянием. Инцидент, с которым живут, принимают через его хозяина — вопрошание-дефект или сбывшийся риск; голое положение дел в `upeksha` ничего о принятии не говорит.

## 4. Вопрошание и превращение (bianhua)

Вимарша — либо **часть превращения**, либо **свободное вопрошание**. Реши, что именно, — сознательно:

- **anga превращения** — её разрешение *двигает* качественное превращение, которое проходит система. Привяжи: `iskron_arrow(action="link", arrow_type="anga", source=<вимарша>, target=<bianhua>)`. Вимарша сохраняет свой `vimarsha_of` (реальный предмет); `anga` добавочен — «моё разрешение двигает это превращение к завершению». Жанр задаёт характер участия: samshaya снимает блокирующую неопределённость, risk защищает bianhua, vyabhichara чинит дефект в нём, оценка prati-paksha его двигает. **Привязывай к существующему лесу** (`lens="bianhua"`); одна вимарша никогда не оправдывает новое превращение — когда ни одно превращение не подходит и ты не уверен, спроси пользователя, а не создавай (имя и телос превращения — поверхность приёмки владельца; скилл assembly).
- **свободное вопрошание** — подлинный открытый вопрос вне идущих превращений. Оставь свободным — но *сознательно*, не по небрежности.

Anga превращения — не только вопросы: `iskron_search(anga_of=<bianhua>)` и фокус `lens="bianhua"` показывают **крии-носители** рядом с вимаршами-драйверами. Вимарша-anga — *путь* (её разрешение двигает перемену); крия-anga — *прибытие*: деяние, которое само составляет превращение. Направление читай из трипути самой крии (нет `anga_kind`): **возведение** входит в ткань (`anagata→vartamana`, `chanda`/`adhimoksha`), **депрекация** покидает её (`vartamana→atita`, `virodha`). Превращение может двигаться сделанным деянием, не только отвеченным вопросом — завершённая крия остаётся anga как прибытие (отдача долга), её `resolved` (из трипути) входит в прогресс bianhua.

Строить карту *из* поля — кластеризовать вимарши в превращения, упорядочивать превращения — скилл **assembly**. Этот скилл решает судьбу каждой вимарши; assembly делает проход по графу.

## 5. Эскалация — что твоё, что нет

| Решаешь сам (рядом с ткачеством) | Выносишь на повестку (address) |
|---|---|
| заякорить висящую вимаршу | отклонить предложение |
| закрыть отвеченный вопрос | расставить приоритеты между вопросами |
| запарковать с сознательным reasoning и условием пробуждения | выбрать-иначе против записанного направления |
| привязать драйвер к превращению | принять нежелательное — риск, дефект, инцидент |
| | всё, что несёт **трансцендентную волю** |

Решения с трансцендентной волей — отказ, приоритет, выбор-иначе, принятие нежелательного — **не** агентские. Выноси их; не разрешай, чтобы расчистить поле. (Та же граница, которую **assembly** проводит между Сборщиком и Мыслепрактиком.)

## 6. Сессия должна прийти к пониманию

Принцип, правящий всем скиллом: **сессия должна сходиться к bildung-пониманиям, а не только плодить вимарши**. Триаж поля, после которого открытых вопросов больше, чем было, и ни одного кристаллизованного прозрения, — не приземлился. Перед закрытием полевой сессии спроси: *что мы теперь понимаем, чего не понимали?* Запиши это (кристаллизуй — исход 3). Это рефинирование, не уборка: сырьё (вимарши, инциденты) → продукт (тезис, крия-паттерн, принцип); visarjana — побочный эффект, не цель. Поле, которое только растёт, — граф, который не учится.

## Приёмка

- Висящее поле реального графа может быть растриажено насквозь *одним этим скиллом*, без обращения к тексту методологии.
- Каждый исход триажа выражается ровно одним ходом из разделов 2–5: заякорить, разрешить, отпустить, кристаллизовать, вытеснить, запарковать, anga, эскалировать.
